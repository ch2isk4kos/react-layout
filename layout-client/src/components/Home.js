import React from 'react';

const Home = () => {
  const { opeeSidebar, openModal } = useGlobalContext();
  return (
    <div className='Home'>
      <main>
        <button>

        </button>
      </main>
    </div>
  )
}

export default Home;
